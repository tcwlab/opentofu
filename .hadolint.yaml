# hadolint configuration for tcwlab/opentofu
#
# DL3018: Pin versions in apk add
# Alpine images intentionally do not pin apk package versions because
# they use the Alpine repository as source-of-truth and update only via
# apk upgrade. Version pinning would break across Alpine releases.
ignore:
  - DL3018
